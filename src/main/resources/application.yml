spring:
  datasource:
    url: jdbc:mysql://localhost:3306/managing-users
    username: root
    password: 4864
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect